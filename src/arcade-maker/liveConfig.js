// Placeholder — stage 1 of modular split. Will hold the full liveConfig definition
// currently inline in tools/arcade-game-maker.html:5480ff. Keeping file here
// proves Vite chunking works (`npx vite build` will emit a liveConfig chunk).
export const liveConfigPlaceholder = { note: 'extracted in stage 1' };
