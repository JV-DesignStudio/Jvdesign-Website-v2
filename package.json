{
  "name": "jvdesignstudio-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:sitemap": "node generate-sitemap.js",
    "validate:links": "node validate-links.js",
    "validate:css": "node validate-css.js",
    "validate": "node validate-links.js && node validate-css.js"
  },
  "devDependencies": {
    "puppeteer": "^25.2.1",
    "sharp": "^0.35.2"
  }
}
